我想为小朋友做一个边接新闻的工具，可以将一个个的汉字的一句话拆成 字，然后用线连接起来。然后能够读出来。应该不是很复杂。
朗读汉字是否有工具？
import sys
 
sys.setdefaultencoding('utf8')
 
import pyttsx
engine = pyttsx.init()
engine.say('hello world')
engine.say('你好吗')
engine.runAndWait()
